{{#each this}}
<li>
    <a href="{{url}}" target="_blank" rel="noopener noreferrer">
    <article>
        <img src="{{urlToImage}}" alt="pictures" width="480">
        <h2>{{title}}</h2>
        <p>Author:{{author}}</p>
        <p>{{description}}</p>
    </article>
    </a>
</li>
{{/each}}
